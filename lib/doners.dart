import 'package:flutter/material.dart';

class Doners extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is Doner list"),
    );
  }
}
