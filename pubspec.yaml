name: bonhi_bohoman
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  line_icons: ^0.2.0
  font_awesome_flutter:
  firebase_database:
  firebase_core:
  percent_indicator:
  in_app_update: ^1.1.7
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
